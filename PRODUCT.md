# PixelForge 产品开发设计方案

> Android 真机可视化自动化 IDE —— 通用型、长期迭代
>
> 版本 1.0 · 2026-09-21

---

## 1. 产品定位

**PixelForge 是一个通用的 Android 真机可视化自动化 IDE。**

它解决一类具体的、跨业务重复出现的痛苦：当你要让程序去操作一个 Android app，你必须回答三个问题 —— 「这个按钮在哪」「我怎么知道页面到了」「它为什么没点中」。今天回答这三个问题的方式是：截图、放到图片工具里量像素、手算归一化坐标、写进配置、跑一遍、失败了再来一轮。

PixelForge 把这个循环压缩成：**看着实时画面点一下**。

### 1.1 一句话定义

```
输入：一台 Android 真机的实时画面
输出：一份可回放、可断点调试、可导出到任意执行引擎的自动化流程
```

### 1.2 它不是什么

| 不是 | 为什么要说清楚 |
|------|---------------|
| 不是某个具体业务的自动化脚本 | 业务逻辑通过**项目**隔离，工具本身不含任何业务知识 |
| 不是 Appium / uiautomator 的替代品 | 它们是**执行引擎**，PixelForge 是它们的**编写与调试前端**，可以导出给它们执行 |
| 不是设备农场管理平台 | 设备管理由 DeviceProvider 插件对接现有平台（DeviceFarmer 等） |
| 不是抓包工具 | 抓包能力通过 Listener 插件接入，PixelForge 只负责把抓包结果和 UI 步骤对齐到一条时间线 |
| 不是云真机服务 | 单机/局域网部署，设备来源可插拔 |

### 1.3 核心价值：三件今天做不到的事

**① 坐标不再靠手算。** 在实时画面上点一下，得到该点在四套坐标空间里的值（CSS / 视频帧 / 设备像素 / 归一化），一键写进步骤。框选一块区域，后端用**无损截图**裁出来存成模板 —— 不是从视频帧裁（H.264 有损，模板会失配）。

**② 脚本可以断点调试。** 跑到第 5 步停住，你手动在真机上拨一格，再点继续。今天调自动化脚本只能"改一行、全跑一遍、看日志猜"。

**③ 点击和网络事件在同一条时间线上。**

```
t=0.00s  step_3  tap(0.48, 0.67)
t=0.31s  [screencap]
t=0.42s  ◀ HTTP   POST /api/market/query
t=0.68s  ◀ HTTP   200 (1.2KB)
t=0.95s  step_4  wait_text("总价")
```

"我点了，但不知道点没点中、请求发没发出去" —— 这是调自动化最费时间的问题，时间线直接回答它。

---

## 2. 目标场景

工具要支撑的是**同一套操作模式，不同的业务对象**。三类典型场景，按对定位能力的要求排序：

| 场景 | 特征 | 主要定位手段 |
|------|------|-------------|
| **A. 普通 app 业务流程** （电商下单、表单填写、账号注册） | 有完整无障碍信息 | 控件选择器（`resource-id` / `text`）为主，跨机型稳定 |
| **B. 游戏 / Canvas / 自绘 UI** （游戏内商店、图表、地图这类） | 无障碍树为空或无意义 | 模板匹配 + OCR，坐标兜底 |
| **C. WebView / 混合应用** | 部分有 a11y，层级怪 | 控件 + 模板混合 |

**设计约束：三类必须由同一套工具覆盖，通过定位策略降级链自动适配，而不是让用户选模式。**

```
① 控件选择器  → ② 模板匹配 → ③ OCR 文本 → ④ 归一化坐标
   最稳，跨机型      无 a11y 时     有文字时    最后兜底
```

录制时**一次性采集全部四种特征**，回放时按顺序尝试，任一命中即停。这是让脚本在不同业务、不同机型间复用的关键机制。

---

## 3. 功能范围

### 3.1 本期范围（P0–P9）

**设备层** · 本地 adb 设备发现与热插拔感知 · 独占租约（多人不互相抢）· DeviceFarmer 等外部设备池对接 · 断线自动重连

**画面层** · H.264 实时投屏（延迟 < 150ms）· 无损按需截图 · 区域框选取材 · 四坐标空间实时显示 · 旋转跟随

**控制层** · 点击 / 长按 / 滑动 / 任意手势（down-move-up）· 多点触控 · 按键 · UTF-8 文本输入（含中文）

**定位层** · UiAutomator2 控件树 Inspector · 点击画面反查控件 · 模板匹配（多尺度 + 掩码）· OCR · 四策略降级链

**脚本层** · Python SDK 编写 · 操作录制自动生成步骤 · 步骤列表可视化编辑 · 变量与凭据引用

**调试层** · 断点 / 单步 / 运行到 / 从某步继续 / 只跑一步 · 暂停时人工接管真机 · 步骤时间线（前后截图 + 匹配分数 + 匹配框）

**集成层** · 项目隔离（每个业务一个项目）· 导出插件（原生 JSON / pytest / 目录加载的第三方插件）· 监听插件（logcat / mitmproxy / pcap）· 统一时间线 · CLI 入口（CI 可跑）

### 3.2 明确的非目标

| 非目标 | 理由 |
|--------|------|
| iOS / 鸿蒙设备 | 底层完全不同（WDA / HDC），预留 DeviceProvider 接口但本期不做 |
| 手机 root | 不作为任何功能的前置条件。仅在 FLAG_SECURE 场景下作为可选说明 |
| 绕过验证码 / 人机校验 | 遇到即触发「人工接管」暂停，不做自动绕过 |
| 绕过证书固定（cert pinning） | mitmproxy 只在目标 app 允许的前提下工作，不集成 Frida hook |
| 通用远程 shell | adb 调用一律 argv 白名单，不接受 shell 命令字符串 |
| 多租户 SaaS | 单机 / 团队内网部署。脚本执行是本机任意代码，不设计对外开放 |
| 群控（同时对 N 台发同一操作） | 每设备独立会话。群控是上层编排的事，不是 IDE 的事 |

---

## 4. 开发阶段周期

**工期口径：** 1 名开发 + AI 辅助，1 工作日 ≈ 6 有效小时。范围值反映"顺利 / 遇到真机适配问题"两种情况。

### 阶段一 · 可视化取材工具（P0–P3）

**目标：彻底替代「截图 + 量像素 + 手算坐标」。这一阶段结束就能日常用起来。**

| 阶段 | 内容 | 工期 | 验收标准 |
|------|------|:---:|---------|
| **P0** 地基 | 项目骨架与工具链；异步 `AdbClient`（argv 白名单、独立 adb server 端口）；`DeviceRegistry`（`host:track-devices` 长连接）；`DeviceLease`（独占租约 + fencing + TTL）；REST 设备接口 | **3–4 天** | 插拔手机，列表**毫秒级**更新（非轮询）；两个客户端抢同一设备，第二个被拒；租约过期后自动释放 |
| **P1** 实时画面 | `ScrcpySession`（推 jar、`app_process` 拉起、双 socket、握手、rotation 事件）；NAL 拆帧；WS 二进制帧广播（多订阅者）；前端 WebCodecs 解码 → canvas；断线重连 | **4–5 天** | 延迟 **< 150ms**（手机秒表对拍实测）；拔线重插自动恢复；两个浏览器同时观看同一设备 |
| **P2** 坐标系统 | `CoordinateMapper`（4 空间 × 旋转 × 8 倍对齐）；**完整单测矩阵**；控制通道（tap / swipe / down-move-up / key / UTF-8 text）；前端坐标十字与实时读数 | **3–4 天** | 屏幕**四角 + 中心**点击误差 **0px**；旋转 90/180/270 后仍然 0px；中文输入成功；点击延迟 **< 20ms** |
| **P3** 取材 | 无损 `screencap` 独立通道（raw RGBA / PNG 双模式实测选型）；前端框选 → 后端裁剪；模板库（命名、ROI、阈值、掩码） | **3–4 天** | 框选区域与设备像素**逐像素一致**（用已知图案比对哈希）；模板落盘可复用 |

**阶段一合计：13–17 天。** 交付物：一个能实时看画面、精确拾取坐标、无损框选取材的工具。

> ⚠️ **P2 和 P3 是全项目成败点。** 坐标偏 3 像素这种 bug 靠人工点击测不出来，只能靠单测矩阵；取材有损会让模板匹配分数无规律漂移，后面所有功能都建立在流沙上。这两个阶段不要赶工期。

### 阶段二 · 脚本 IDE（P4–P6）

**目标：从「能取材」到「能录制、回放、调试完整业务流程」。**

| 阶段 | 内容 | 工期 | 验收标准 |
|------|------|:---:|---------|
| **P4** 元素定位 | UiAutomator2 server 常驻集成（**不用** `uiautomator dump`）；控件树 Inspector；点击画面反查控件；模板匹配（多尺度 + 掩码 + 画面稳定检测）；OCR；四策略降级链 | **4–5 天** | 场景 A（普通 app）点画面能读出 `resource-id`；场景 B（游戏）模板匹配能命中；取控件树 **< 500ms** 且不阻塞画面 |
| **P5** 脚本引擎 | PixelForge 自有 Step schema；`dev` SDK（**SDK 调用即步骤边界**）；Executor（asyncio task + 事件流）；操作录制自动生成步骤；步骤列表可视化编辑 | **5–6 天** | 录制一个含登录的流程，**回放 10 次全部成功**；录制产物可在 UI 里拖拽重排 |
| **P6** 调试器 | 断点 / 单步 / 运行到 / 从某步继续 / 只跑一步；暂停时人工接管真机；步骤时间线（前后截图 + 匹配分数 + 匹配框）；可选 `debugpy` attach | **4–5 天** | 断点处手动操作真机后继续，脚本状态正确；失败步骤能一眼区分「没找到」vs「找到了但点偏」 |

**阶段二合计：13–16 天。** 交付物：完整的脚本编写与调试 IDE。

### 阶段三 · 通用化与工程化（P7–P9）

**目标：从「单人能用」到「多业务、可交付、可进 CI」。**

| 阶段 | 内容 | 工期 | 验收标准 |
|------|------|:---:|---------|
| **P7** 项目与导出 | 项目概念（每业务一套设备池/模板库/脚本/变量）；`Exporter` 插件接口与目录加载；原生 JSON 与 pytest 导出器 | **3–4 天** | 第三方插件能从配置目录加载并注册；导出的 pytest 能在 CI 跑；降级处必须报警 |
| **P8** 监听与时间线 | `Listener` 插件接口；logcat listener；mitmproxy listener；pcap/UDP listener 接口；统一时间线对齐 | **4–5 天** | 点击后能在时间线看到对应网络事件，时间偏差 < 100ms |
| **P9** 工程化 | 鉴权；多设备并发压测；CLI 入口；部署文档 + Docker；错误处理与可观测性 | **4–5 天** | **3 台设备同时运行互不干扰**；CLI 无头执行脚本；冷启动文档能让另一个人独立部署成功 |

**阶段三合计：11–14 天。**

### 总计

| | 工期 | 累计 |
|---|:---:|:---:|
| 阶段一 · 可视化取材工具 | 13–17 天 | 13–17 天 |
| 阶段二 · 脚本 IDE | 13–16 天 | 26–33 天 |
| 阶段三 · 通用化与工程化 | 11–14 天 | **37–47 天** |

**≈ 8–10 周（单人）。**

### 4.1 两个可提前使用的节点

不必等全部完成。有两个节点交付物已经独立有用：

**① P3 结束（13–17 天）—— 取材工具可用。**
这时你还不能录脚本，但已经能实时看画面、点一下拿到精确坐标、框选取无损素材。手算坐标这件事从此消失。**建议在这里停下来实际用一周**，用真实业务反馈校正 P4+ 的设计 —— 比按原计划闷头做完更省时间。

**② P6 结束（26–33 天）—— IDE 可用。**
录制、回放、断点调试齐备。可以开始把存量脚本迁进来。P7–P9 是多业务扩展和交付质量，不阻塞单人使用。

### 4.2 工期风险

| 风险 | 概率 | 影响 | 对策 |
|------|:---:|------|------|
| scrcpy 在某型号真机上握手失败 | 中 | P1 +2 天 | P1 开始前先在**全部目标机型**上手动跑一遍 scrcpy 命令行确认 |
| 坐标在某些厂商 ROM 上有异常偏移（异形屏、强制 DPI） | 中 | P2 +2–3 天 | 单测矩阵 + 每新增一个机型跑一次校准图案 |
| UiAutomator2 server 与目标 app 冲突 | 低 | P4 +2 天 | 降级到模板匹配，不阻塞主流程 |
| WebCodecs 在目标浏览器不可用 | 低 | P1 +3 天 | 明确只支持 Chrome/Edge；备选 MSE 方案但延迟会升到 300ms+ |
| **目标 app 设了 FLAG_SECURE** | 低（但对通用工具**不可排除**） | 该业务**无法支持** | 见 §5 |

---

## 5. FLAG_SECURE：通用工具必须正面处理的边界

作为通用工具，这个风险**不能像单业务那样排除掉**。

Android 12 起，SurfaceFlinger 的截屏路径检查 `CAPTURE_BLACKOUT_CONTENT` 权限并把 shell 移出白名单。带 `FLAG_SECURE` 的窗口在 **scrcpy、minicap、`screencap` 三者中一律黑屏** —— 这是框架级限制，不是工具缺陷。银行 app、支付页、DRM 视频都会命中。

**产品决策：不绕过，但要诊断清楚。**

P0 阶段就实现一个 `secure_window_probe`：截图后检测「整屏或大块区域纯黑 + 同时 UiAutomator2 能取到非空控件树」这个组合特征 —— 那就是 FLAG_SECURE，而不是真的黑屏。前端明确提示：

> 此页面受 `FLAG_SECURE` 保护，无法获取画面。控件树仍可用，可使用控件选择器定位；坐标拾取与模板匹配不可用。

这样用户 5 秒内知道原因，而不是花两小时怀疑自己配错了。**这类"把无解的问题快速诊断清楚"的设计，是通用工具和一次性脚本的分水岭。**

---

## 6. 技术栈

| 层 | 选型 | 理由 |
|---|------|------|
| 后端 | Python 3.12 + FastAPI + asyncio（**单进程**） | 设备会话是进程内状态，多 worker 会让请求落到没有该会话的进程上 |
| 投屏 | scrcpy-server（vendor 进仓库，锁版本） | 走公开 `MediaCodec` API，Android 8→16 通吃；minicap 需按版本×ABI 预编译 `.so` |
| 控件 | appium-uiautomator2-server（常驻） | `uiautomator dump` 要 1–3s 且动画时报 "could not get idle state" 直接失败 |
| 前端 | React + TypeScript + Vite | — |
| 解码 | WebCodecs `VideoDecoder` | 后端零 CPU（只拆帧不解码），延迟 50–120ms；MSE 压不下来，WebRTC 过度设计 |
| CV | OpenCV + pytesseract（**ProcessPoolExecutor**） | 单次几百毫秒纯 CPU，跑在事件循环里会卡住所有设备的画面 |
| 存储 | SQLite（元数据）+ 文件系统（截图/模板/录像） | 单机部署，无需外部依赖 |
| 设备池 | DeviceProvider 插件：local adb / DeviceFarmer | 复用已部署的 DeviceFarmer，不重复造设备管理 |

**两条必须遵守的工程约束**（违反会在多设备下卡死，且很难定位）：

1. adb 调用一律 `asyncio.create_subprocess_exec`，绝不 `subprocess.run`
2. adb 二进制 vendor 进仓库并用**独立 server 端口**（`-P 5038`）—— 默认 5037 是全局单例，同事开一次 Android Studio 或任何人 `adb kill-server`，你所有设备会话瞬间全断

---

## 7. 与下游执行引擎的关系

下游项目是 PixelForge 的**消费者**，不是它的依赖。

```
PixelForge  ──导出──▶  目标 profile 格式  ──▶  下游引擎执行
（编写调试）              （交付产物）              （生产运行）
```

**依赖方向必须单向：PixelForge 不 import 任何下游业务模块。** 反向依赖会让通用工具被一个业务的 schema 绑死——下游引擎的步骤格式通常刻意做得很窄（固定 action 枚举、`extra="forbid"`、仅坐标定位），因为下发到边缘节点执行的 profile 不该能携带任意行为，这是正当的安全决策。但把这样一份 schema 当成 PixelForge 的内部模型，等于让第二个业务一进来就得改公共 schema。

具体做法：PixelForge 有自己更富的 Step 模型，下游格式通过 **Exporter 插件**对接。插件放在 `PIXELFORGE_EXPORTER_PLUGINS` 指向的目录里，核心的注册表只保留与业务无关的两个导出器。

转换不了的地方必须**明确报错，而不是悄悄丢掉**：

```
⚠ 步骤 4 使用模板匹配定位，目标 profile 不支持 wait_image。
  可选：① 改用 OCR 文本定位  ② 降级为坐标  ③ 扩展目标引擎的 action 枚举
```

这条提示本身就是有价值的产出——它告诉你目标引擎该往哪扩展。静默丢信息会产出**在 IDE 里通过、在生产里少做事**的脚本，这是最糟的失败形态。
